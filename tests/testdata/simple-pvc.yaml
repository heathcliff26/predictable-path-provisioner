---
apiVersion: v1
kind: Namespace
metadata:
  name: test-simple-pvc
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: default-sc
  namespace: test-simple-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Mi
---
kind: Pod
apiVersion: v1
metadata:
  name: test-pod
  namespace: test-simple-pvc
spec:
  containers:
    - name: fedora
      image: registry.fedoraproject.org/fedora:latest
      command:
        - /bin/bash
        - -c
        - |
          echo "This is a test file for the simple PVC" > /data/test-file
          ls -l /data
          echo "Done"
      volumeMounts:
        - name: data
          mountPath: /data
  restartPolicy: OnFailure
  volumes:
    - name: data
      persistentVolumeClaim:
        claimName: default-sc
